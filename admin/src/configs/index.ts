export const paths = {
  root: "/",
  login: "login",
  register: "register",
};

export const authFullPaths = {
  "root.login": "/login",
  "root.register": "/register",
};

export const fullPaths = {
  root: "/",
  ...authFullPaths,
};
