{
  "name": "nocode-builder",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "remote::start": "node ./scripts/start-with-version.js",
    "remote::pull-images": "node ./scripts/pull-images.js",
    "format": "prettier --write \"*/src/**/*.(ts|tsx)\"",
    "image:tag-and-push-remote": "node ./scripts/tag-and-push-remote.js",
    "image:build-and-tag": "node ./scripts/image-build-and-tag.js",
    "docker": "docker-compose up --build -d --remove-orphans",
    "docker:db": "docker-compose up postgres --build -d --remove-orphans",
    "docker:server": "docker-compose up server --build -d --remove-orphans",
    "cypress:run": "pnpm --filter admin cypress:run",
    "dev:e2e": "pnpm --filter admin cypress:open",
    "dev:admin": "pnpm --filter admin dev",
    "dev:server": "pnpm --filter server start:dev",
    "everyDay": "pnpm format && pnpm --filter server recursive run everyDay && pnpm --filter=!server recursive run everyDay && pnpm docker && pnpm cypress:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "execa": "^9.3.0",
    "glob": "^10.4.2",
    "js-yaml": "^4.1.0",
    "minimatch": "^10.0.1",
    "prettier": "^3.0.0",
    "yaml": "^2.4.5"
  }
}
